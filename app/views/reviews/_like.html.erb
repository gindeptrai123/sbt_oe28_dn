<div class="box-like">
  <%= @review.likes.size %> <i class="fa fa-thumbs-up"></i> <br />
  <%= form_for @like, url: like_path, remote: true do |f| %>
    <%= f.hidden_field :review_id, value: @review.id  %>
    <button class="btn btn-primary"><%= t "like_review" %></button>
  <% end %>
</div>
